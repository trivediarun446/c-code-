// write a c programe to marge to two array
#include <stdio.h>
int main(){
    int n ,k,i,j;
    int arr1[n];
    int marge[n+j];
    printf("enter the number of element in an array1 :");
    scanf("%d",&n);
    for (int i = 0; i < n; i++)
    {
        printf("arr1[%d]:",i);
        scanf("%d",arr1[i]);
        marge[k]=arr1[i];
    }
    int j;
    int arr2[j];
    printf("enter the number of element in an array2 :");
    scanf("%d",&j);
    
   for (int i= 0;  i< j; i++)
   {
    printf("arr2[%d]",i);
    scanf("%d",&arr2[i]);
   }
   
    
}